/**
 * 用户相关逻辑
 */

// 用户注册controller

const userRegister = (username, password) => {};

// 用户登录controller
const userLogin = (username, password) => {};

module.exports = {
	userRegister,
	userLogin,
};
